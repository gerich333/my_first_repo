# Подсказка по Git

Создание репозитория:
```sh
git init
```

Выводит версию Git:
```sh
git --version
```

Выводит в терминал статус Git:
```sh
git status
```

Добавление измененного файла в индекс:
```sh
git add <file name>
```

Фиксация изменения файла в репозиторий:
```sh
git commit -m "Message"
```

Выводит список всех коммитов:
```sh
git log
```

Выводит список всех коммитов в укороченном виде:
```sh
git log --oneline
```

Выводит список всех коммитов в укороченном виде c отображением веток:
```sh
git log --oneline --graph
```

Переключение между коммитами / ветками:
```sh
git checkout <Hash commits> / <branch name>
```

Отображение всех веток
```sh
git branch
```

Создание новой ветки
```sh
git branch <name_new_branch>
```

Удаление ветки
```sh
git branch -d <name_branch>
```